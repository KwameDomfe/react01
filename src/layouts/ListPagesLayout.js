// import { NavLink, Outlet } from "react-router-dom"

export default function ListPagesLayout(props) {
    
    const {lipname} = props

    return (

        <div id="careers"
            className="h-100 ph1-00"
        >
            <h2>{lipname} Lists Page</h2>
        </div>
        
    )
}
